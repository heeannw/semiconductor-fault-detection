server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # React Router(BrowserRouter)의 클라이언트 사이드 라우팅을 위해
    # 알 수 없는 경로는 전부 index.html로 폴백한다.
    location / {
        try_files $uri /index.html;
    }
}
